{
  "schema": "headless-pr-workflow.required-check-policy.v1",
  "branches": {
    "main": {
      "required_status_checks": "absent",
      "ci_workflows": "absent",
      "source": "docs/MERGE-POLICY.md#main-required-check-policy",
      "rationale": "This repository intentionally has no CI workflows and no required status checks for main. Required-check gating may treat unavailable branch-protection required-check data as policy-absent only when no failing, pending, unknown, or missing checks are reported."
    }
  }
}
